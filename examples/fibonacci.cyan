// ── Fibonacci ──────────────────────────────────────────────────────
// Print the Nth Fibonacci number.

function fib(n) {
    if (n <= 1) {
        return(n);
    }
    return(fib(n - 1) + fib(n - 2));
}

var result = fib(12);
print(result);
return(0);
